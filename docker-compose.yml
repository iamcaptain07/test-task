version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    # Environment variables are baked into the image from .env file
    # Add additional env vars here if needed at runtime
    restart: unless-stopped

